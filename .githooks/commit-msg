#!/bin/sh
# Enforced by npm's `prepare` script pointing core.hooksPath here; CI runs the
# same check over every pull request commit and title.
npx --yes \
  --package @commitlint/cli@21.2.2 \
  commitlint --edit "$1"
